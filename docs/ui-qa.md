## UI QA checklist

- Guest: menu load, add item, open cart, edit qty/remove, send to kitchen, pay, call waiter, request bill, and toasts on error.
- Kitchen: see new ticket, move status through columns, empty states show CTA, errors toast via `toastApiError`.
- Admin/owner: list pages show filters and empty states with CTA, errors toast via `toastApiError`.
