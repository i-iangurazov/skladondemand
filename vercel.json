{
  "builds": [
    { "src": "apps/web/package.json", "use": "@vercel/next" }
  ],
  "installCommand": "pnpm -w install",
  "buildCommand": "pnpm exec prisma generate --schema ../../packages/db/prisma/schema.prisma && pnpm build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
