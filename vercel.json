{
  "builds": [
    { "src": "apps/web/package.json", "use": "@vercel/next" }
  ],
  "installCommand": "pnpm -w install",
  "buildCommand": "pnpm --filter @qr/db exec prisma generate && pnpm --filter @store/web build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
