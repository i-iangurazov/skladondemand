{
  "builds": [
    { "src": "apps/web/package.json", "use": "@vercel/next" }
  ],
  "installCommand": "pnpm -w install",
  "buildCommand": "pnpm --filter @qr/db prisma:generate && pnpm --filter @store/web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
